# import * from './generated/prisma.graphql'

type Mutation {
  signup(
    email: String!
    password: String!
    firstName: String
    lastName: String
  ): User
  signin(email: String, password: String): User
}

type Query {
  allUsers: [User]
}
